#include "lexical_anaysis.hpp"
int main()
{
    complier::LexicalAnaysis la;
    la.anaysis();
    /* std::cout << std::endl; */
    /* la.anaysis_predict({"id", "+", "id", "*", "id", "$"}); */
    return 0;
}
